htaccessの使い方

特徴
初めに宣言を行う  allowoverride all
配置したディレクトリ、サブディレクトリで有効となる（ディレクトリの細分化した先まで）
上位で設定したものは下位で設定したもので上書きされる

 allowoverride allを有効にするとすべてのフォルダに対して、htaccessの存在を確認しにいく
 何もなければnoneで固定しておく

 htaccessの有効範囲

使い方
directoryテキストとは異なり、変更を加えたいディレクトリ階層にて.htaccessを作成しその中に
変更（ディレクトリ）を記述する